#include "sim/sim.hpp"
#include "algo.hpp"

// fills the waypoints/paths in map
void Map::pathfind() {
    std::cout << "\ncalling pathfind\n";

    // pass acosrar bounds
    

    // path = std::move(algo.archive->solutions[0].waypoints);

    std::cout << "\ncalled pathfind\n";
}